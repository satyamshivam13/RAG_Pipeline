---
phase: 02-measure-observe
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - evaluation/quality_gates.py
  - .github/workflows/quality-regression.yml
  - tests/test_quality_gates.py
  - README.md
autonomous: true
requirements:
  - MEAS-01
  - MEAS-02
  - MEAS-03
  - MEAS-04
must_haves:
  truths:
    - "CI fails when faithfulness or answer relevancy regress below agreed thresholds."
    - "CI consumes the reproducible evaluation report generated by the Phase 2 eval command."
    - "Quality-gate behavior is test-covered and documented for local reproduction."
  artifacts:
    - path: "evaluation/quality_gates.py"
      provides: "Threshold evaluation and non-zero exit on gate failures"
    - path: ".github/workflows/quality-regression.yml"
      provides: "Automated evaluation + gate execution in CI"
    - path: "tests/test_quality_gates.py"
      provides: "Regression tests for pass/fail threshold logic"
  key_links:
    - from: ".github/workflows/quality-regression.yml"
      to: "evaluation/run_eval.py"
      via: "evaluation report generation step"
      pattern: "python -m evaluation.run_eval"
    - from: "evaluation/quality_gates.py"
      to: "evaluation/reports/*.json"
      via: "parsed metric report"
      pattern: "faithfulness|answer_relevancy"
---

<objective>
Add CI quality regression gates on top of the new evaluation and telemetry baseline.

Purpose: Prevent silent quality regressions and make Phase 2 outcomes enforceable.
Output: Quality gate script, CI workflow, tests, and usage docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-measure-observe/02-CONTEXT.md
@.planning/phases/02-measure-observe/02-RESEARCH.md
@.planning/phases/02-measure-observe/02-01-PLAN.md
@.planning/phases/02-measure-observe/02-02-PLAN.md
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement quality gate evaluator for Phase 2 threshold policy</name>
  <read_first>evaluation/run_eval.py, .planning/phases/02-measure-observe/02-CONTEXT.md, README.md</read_first>
  <files>evaluation/quality_gates.py, tests/test_quality_gates.py</files>
  <behavior>
    - Test 1: Gate fails when faithfulness is below 0.90 (per D-09).
    - Test 2: Gate fails when answer relevancy is below 0.95 (per D-10).
    - Test 3: Context precision and recall are reported but do not fail CI in this phase (per D-11).
  </behavior>
  <action>Create `evaluation/quality_gates.py` CLI that reads report JSON and enforces `faithfulness >= 0.90` and `answer_relevancy >= 0.95` with non-zero exit when violated. Include non-blocking printed diagnostics for `context_precision` and `context_recall`. Add `tests/test_quality_gates.py` for pass-path, each fail-path, and missing-key parser error handling.</action>
  <verify>
    <automated>pytest tests/test_quality_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `evaluation/quality_gates.py` contains explicit numeric thresholds 0.90 and 0.95.
    - Gate exits non-zero when either required threshold is violated.
    - Context precision/recall are logged but do not trigger failure.
    - `pytest tests/test_quality_gates.py -q` exits 0.
  </acceptance_criteria>
  <done>Quality gate behavior is deterministic, test-covered, and aligned with locked threshold policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CI workflow to run evaluation and enforce quality gates</name>
  <read_first>evaluation/run_eval.py, evaluation/quality_gates.py, requirements.txt, .planning/phases/02-measure-observe/02-CONTEXT.md</read_first>
  <files>.github/workflows/quality-regression.yml, tests/test_quality_gates.py</files>
  <behavior>
    - Test 1: Workflow installs dependencies and runs the reproducible eval command from Phase 2.
    - Test 2: Workflow fails when quality gate script exits non-zero.
    - Test 3: Workflow artifacts include metric report for inspection.
  </behavior>
  <action>Create `.github/workflows/quality-regression.yml` that runs on pull_request, installs dependencies, runs `python -m evaluation.run_eval --dataset evaluation/datasets/phase2_eval.jsonl --output evaluation/reports/phase2-latest.json`, runs `python -m evaluation.quality_gates --report evaluation/reports/phase2-latest.json`, and uploads `evaluation/reports/phase2-latest.json` as artifact.</action>
  <verify>
    <automated>pytest tests/test_quality_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Workflow includes separate eval and quality-gate steps with explicit commands.
    - Workflow uploads the generated report artifact.
    - Quality gate step controls workflow pass/fail outcome.
  </acceptance_criteria>
  <done>CI enforces agreed quality thresholds and publishes evaluation outputs for debugging and trend tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Document local quality-gate reproduction path</name>
  <read_first>README.md, evaluation/run_eval.py, evaluation/quality_gates.py</read_first>
  <files>README.md</files>
  <action>Update README with a "Phase 2 Quality Regression" section that includes exact local commands: `python -m evaluation.run_eval --dataset evaluation/datasets/phase2_eval.jsonl --output evaluation/reports/phase2-latest.json` and `python -m evaluation.quality_gates --report evaluation/reports/phase2-latest.json`, plus threshold notes (`faithfulness >= 0.90`, `answer_relevancy >= 0.95`).</action>
  <verify>
    <automated>pytest tests/test_quality_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - README contains a dedicated Phase 2 quality regression section.
    - Section includes both local commands and exact threshold values.
    - Command text matches CI workflow command text.
  </acceptance_criteria>
  <done>Developers can run the same evaluation and gate flow locally before pushing changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval report->gate parser | Parsed metric values determine CI pass/fail outcomes |
| CI workflow->repository code | Untrusted changes can alter gate script behavior |
| local developer run->published thresholds | Human workflows may diverge from CI if undocumented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | evaluation/quality_gates.py | mitigate | Parse known metric keys with strict validation and fail on missing/invalid values. |
| T-02-10 | R | CI gate outcomes | mitigate | Emit explicit threshold comparisons in output and persist report artifacts for auditability. |
| T-02-11 | D | CI runtime cost/latency | accept | Full eval runtime is acceptable for v1; future optimization can shard or sample runs. |
| T-02-12 | E | gate bypass by local-only checks | mitigate | Enforce gates in mandatory CI workflow, not only developer-local commands. |
</threat_model>

<verification>
- Run: pytest tests/test_quality_gates.py -q
- Run: python -m evaluation.run_eval --dataset evaluation/datasets/phase2_eval.jsonl --output evaluation/reports/phase2-latest.json
- Run: python -m evaluation.quality_gates --report evaluation/reports/phase2-latest.json
- Confirm CI workflow triggers on PR and fails when thresholds are violated.
</verification>

<success_criteria>
- CI quality regression checks are active and enforce locked thresholds.
- Phase 2 evaluation outputs are consumed by automation and exposed as artifacts.
- Local and CI quality checks use the same command path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-measure-observe/02-03-SUMMARY.md`
</output>
