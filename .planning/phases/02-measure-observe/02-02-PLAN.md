---
phase: 02-measure-observe
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - telemetry.py
  - main.py
  - llm_client.py
  - retriever.py
  - generator.py
  - evaluator_agent.py
  - tests/test_telemetry.py
autonomous: true
requirements:
  - MEAS-03
  - MEAS-04
must_haves:
  truths:
    - "Each query run has a correlation ID present in structured logs across pipeline stages."
    - "Tracing spans capture retrieval, generation, and evaluation under one root query span."
    - "Telemetry export works by default locally and is configurable for OTLP collectors."
  artifacts:
    - path: "telemetry.py"
      provides: "Shared correlation-id context and tracing setup helpers"
    - path: "main.py"
      provides: "Root query span and stage-level span orchestration"
    - path: "tests/test_telemetry.py"
      provides: "Automated checks for structured logs and span creation"
  key_links:
    - from: "main.py"
      to: "telemetry.py"
      via: "query context bootstrap + span lifecycle"
      pattern: "span|correlation"
    - from: "llm_client.py"
      to: "telemetry.py"
      via: "propagated correlation metadata in log events"
      pattern: "logger"
---

<objective>
Implement Phase 2 observability with structured logs and end-to-end tracing spans.

Purpose: Make per-query runtime behavior diagnosable and measurable across components.
Output: Telemetry helpers, instrumentation across pipeline stages, and regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-02-measure-observe/02-CONTEXT.md
@config.py
@main.py
@llm_client.py
@retriever.py
@generator.py
@evaluator_agent.py

<interfaces>
From main.py:
```python
class RAGPipeline:
    def query(self, question: str) -> PipelineResult: ...
```

From llm_client.py:
```python
class LLMClient:
    def chat(self, messages: list[dict[str, str]], ...) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared telemetry configuration and correlation context helpers</name>
  <files>config.py, telemetry.py, tests/test_telemetry.py</files>
  <behavior>
    - Test 1: Query-scoped correlation IDs are generated when missing and retrievable from context (per D-06).
    - Test 2: Telemetry config defaults to console export and can switch to OTLP via environment/config (per D-08).
    - Test 3: Helper APIs are deterministic and safe to call repeatedly.
  </behavior>
  <action>Create a dedicated telemetry module with context-local correlation-id utilities and OpenTelemetry setup helpers. Add config switches for exporter mode and service name with default console behavior and OTLP-ready wiring (per D-08). Add unit tests for context propagation and configuration behavior.</action>
  <verify>
    <automated>pytest tests/test_telemetry.py -q</automated>
  </verify>
  <done>Telemetry foundation exists with low-friction defaults and production-ready exporter configuration paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument query orchestration with structured logs and stage spans</name>
  <files>main.py, retriever.py, generator.py, evaluator_agent.py, tests/test_telemetry.py</files>
  <behavior>
    - Test 1: Structured log events for query lifecycle include correlation_id and per-stage latency fields (per D-06).
    - Test 2: Root query span contains child spans for retrieval, generation, and evaluation (per D-07).
    - Test 3: Retrieval metadata needed by roadmap exit criteria is attached to logs or span attributes.
  </behavior>
  <action>Update pipeline execution logging to structured fields and propagate correlation_id from query entry through stage execution (per D-06). Wrap stage execution with tracing spans (query root plus retrieval/generation/evaluation children) and attach key attributes for timing and retrieval metadata (per D-07).</action>
  <verify>
    <automated>pytest tests/test_telemetry.py -q</automated>
  </verify>
  <done>Query traces and logs now provide end-to-end visibility for runtime behavior and latency diagnosis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend client/stage logging with correlation-aware context propagation checks</name>
  <files>llm_client.py, retriever.py, generator.py, evaluator_agent.py, tests/test_telemetry.py</files>
  <behavior>
    - Test 1: LLM calls emit structured logs carrying the active correlation_id.
    - Test 2: Stage logs remain correlated under both deferred and sync evaluator modes.
  </behavior>
  <action>Instrument stage and LLM client logging paths so all component logs include correlation metadata and consistent event naming. Validate propagation in tests for both deferred and sync evaluator paths to avoid observability blind spots.</action>
  <verify>
    <automated>pytest tests/test_telemetry.py tests/test_pipeline_phase1_runtime.py -q</automated>
  </verify>
  <done>Component-level logs and traces are consistently correlated across normal and fallback execution modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query input->log fields | Untrusted input can be emitted in logs if not controlled |
| pipeline spans->telemetry exporter | Runtime metadata crosses process boundary to external collector |
| correlation context->threaded evaluator | Context propagation crosses worker boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | I | structured logs in stage modules | mitigate | Use explicit structured fields and avoid dumping full prompt/context payloads. |
| T-02-06 | R | correlation propagation | mitigate | Centralize correlation-id helpers and test propagation across deferred and sync paths. |
| T-02-07 | D | tracing/export path | accept | Exporter outages do not block query path; telemetry failures are non-fatal in v1. |
| T-02-08 | E | telemetry config misuse | mitigate | Restrict exporter mode to typed config values and safe defaults (console). |
</threat_model>

<verification>
- Run: pytest tests/test_telemetry.py tests/test_pipeline_phase1_runtime.py -q
- Run: python demo.py and verify query logs include correlation_id and stage latency fields.
- Optional: set OTEL_EXPORTER_OTLP_ENDPOINT and verify spans emit without code changes.
</verification>

<success_criteria>
- MEAS-03 structured logging with correlation IDs is implemented.
- MEAS-04 tracing spans cover retrieval, generation, and evaluation.
- Telemetry export defaults locally and is collector-ready by config.
</success_criteria>

<output>
After completion, create `.planning/phases/02-measure-observe/02-02-SUMMARY.md`
</output>
