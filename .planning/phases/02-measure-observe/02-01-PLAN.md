---
phase: 02-measure-observe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - evaluation/__init__.py
  - evaluation/dataset.py
  - evaluation/run_eval.py
  - tests/test_eval_dataset.py
  - tests/test_eval_runner.py
autonomous: true
requirements:
  - MEAS-01
  - MEAS-02
must_haves:
  truths:
    - "Offline evaluation dataset exists in-repo and is deterministic to run repeatedly."
    - "Evaluation command outputs faithfulness, answer relevancy, context precision, and context recall."
    - "Evaluation run produces a machine-readable report file suitable for CI consumption."
  artifacts:
    - path: "evaluation/dataset.py"
      provides: "Dataset loading and schema validation for JSONL evaluation rows"
    - path: "evaluation/run_eval.py"
      provides: "Reproducible offline evaluation command and report emission"
    - path: "tests/test_eval_runner.py"
      provides: "Regression checks for metric/report generation behavior"
  key_links:
    - from: "evaluation/run_eval.py"
      to: "main.py"
      via: "Pipeline query execution during offline eval"
      pattern: "RAGPipeline"
    - from: "evaluation/run_eval.py"
      to: "evaluation/dataset.py"
      via: "Validated dataset ingestion"
      pattern: "load|validate"
---

<objective>
Create the offline evaluation foundation and reproducible metric command.

Purpose: Satisfy the Phase 2 measurement baseline with deterministic data and report outputs.
Output: Evaluation package, dataset schema/loader, CLI evaluation runner, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-02-measure-observe/02-CONTEXT.md
@main.py
@models.py
@requirements.txt

<interfaces>
From main.py:
```python
class RAGPipeline:
    def query(self, question: str) -> PipelineResult: ...
```

From models.py:
```python
class PipelineResult(BaseModel):
    answer: str
    retrieval: list[RetrievedChunk]
    generation: GeneratorOutput
    evaluation: EvaluatorOutput
    total_time_ms: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add evaluation dataset contracts and deterministic loader</name>
  <files>evaluation/__init__.py, evaluation/dataset.py, tests/test_eval_dataset.py</files>
  <behavior>
    - Test 1: JSONL rows with required fields parse into typed evaluation samples (per D-01, D-02).
    - Test 2: Missing or malformed required fields fail with actionable validation errors.
    - Test 3: Loader preserves deterministic row ordering for repeatable runs.
  </behavior>
  <action>Create evaluation dataset contracts and a JSONL loader for mixed hand-labeled plus synthetic examples (per D-01, D-02). Enforce required fields needed for RAGAS metrics, keep ordering deterministic, and add unit tests for happy-path and validation failures.</action>
  <verify>
    <automated>pytest tests/test_eval_dataset.py -q</automated>
  </verify>
  <done>Dataset schema and loader are deterministic, validated, and suitable for repeatable evaluation runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reproducible RAGAS evaluation command and report output</name>
  <files>requirements.txt, evaluation/run_eval.py, tests/test_eval_runner.py</files>
  <behavior>
    - Test 1: Evaluation command computes faithfulness, answer relevancy, context precision, and context recall (per D-04).
    - Test 2: Command writes a structured report file containing metric values and run metadata (per D-05).
    - Test 3: Re-running with identical dataset and stubs produces stable output shape.
  </behavior>
  <action>Add RAGAS dependency wiring and implement a CLI entry script that loads the dataset, executes pipeline queries, computes Phase 2 metric set (per D-04), and writes a deterministic JSON report to disk (per D-05). Keep command scriptable for local and CI use.</action>
  <verify>
    <automated>pytest tests/test_eval_runner.py -q</automated>
  </verify>
  <done>Offline evaluation can be run via one command and produces reproducible metric reports for downstream gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add baseline dataset seed file and command-level smoke regression</name>
  <files>evaluation/dataset.py, evaluation/run_eval.py, tests/test_eval_dataset.py, tests/test_eval_runner.py</files>
  <behavior>
    - Test 1: Baseline dataset count and schema checks enforce initial target size constraints (per D-03).
    - Test 2: Command-level smoke run completes and emits expected metric keys.
  </behavior>
  <action>Add a baseline seed path contract for the in-repo dataset and enforce count/schema assertions aligned with the initial 150-example target (per D-03). Extend tests with a command-level smoke flow using lightweight stubs.</action>
  <verify>
    <automated>pytest tests/test_eval_dataset.py tests/test_eval_runner.py -q</automated>
  </verify>
  <done>Phase 2 measurement baseline is available and guarded by regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset file->evaluation loader | Untrusted row content crosses into metric pipeline |
| evaluation runner->pipeline query | Batch offline inputs invoke runtime query logic |
| report output->CI consumer | Metric report is parsed by automated gate logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | evaluation/dataset.py | mitigate | Strict schema validation and explicit required fields reject malformed rows before metric execution. |
| T-02-02 | D | evaluation/run_eval.py | mitigate | Bounded evaluation loops with deterministic sample iteration and clear failure handling prevent runaway runs. |
| T-02-03 | R | report reproducibility | mitigate | Emit run metadata and stable report schema; add tests for deterministic output shape. |
| T-02-04 | I | evaluation report contents | accept | v1 report excludes raw context payload dumps; detailed data minimization hardening is deferred. |
</threat_model>

<verification>
- Run: pytest tests/test_eval_dataset.py tests/test_eval_runner.py -q
- Run: python -m evaluation.run_eval --dataset evaluation/datasets/phase2_eval.jsonl --output evaluation/reports/phase2-latest.json
- Confirm report JSON includes: faithfulness, answer_relevancy, context_precision, context_recall.
</verification>

<success_criteria>
- In-repo offline dataset and deterministic loader exist.
- RAGAS metric suite for MEAS-02 is computed from one reproducible command.
- Evaluation report output is machine-readable and CI-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/02-measure-observe/02-01-SUMMARY.md`
</output>
