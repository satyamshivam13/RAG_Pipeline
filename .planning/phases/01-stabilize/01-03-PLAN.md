---
phase: 01-stabilize
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - embeddings.py
  - vector_store.py
  - tests/test_vector_store.py
  - tests/test_embeddings.py
autonomous: true
requirements:
  - RETR-01
must_haves:
  truths:
    - "Default embedding model is upgraded to BAAI/bge-large-en-v1.5."
    - "Dimension mismatches between embeddings and index fail fast with clear errors."
    - "Index persistence/load paths reject incompatible dimensions explicitly."
  artifacts:
    - path: "embeddings.py"
      provides: "Embedding model default and strict output-dimension validation"
    - path: "vector_store.py"
      provides: "Index dimension compatibility checks on add/load paths"
    - path: "tests/test_embeddings.py"
      provides: "Embedding dimension/default model regression tests"
  key_links:
    - from: "embeddings.py"
      to: "config.py"
      via: "EmbeddingConfig default model/dimension"
      pattern: "EmbeddingConfig"
    - from: "main.py"
      to: "vector_store.py"
      via: "VectorStore initialized with embedding dimension"
      pattern: "VectorStore\("
---

<objective>
Upgrade embedding defaults and enforce strict dimension/index compatibility checks.

Purpose: Improve retrieval quality baseline and eliminate silent dimension drift failures.
Output: Updated embedding/vector-store validations and targeted tests for mismatch handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase-01-stabilize/01-CONTEXT.md
@embeddings.py
@vector_store.py
@tests/test_vector_store.py

<interfaces>
From embeddings.py:
```python
class EmbeddingModel:
    def embed(self, texts: list[str]) -> np.ndarray: ...
    @property
    def dimension(self) -> int: ...
```

From vector_store.py:
```python
class VectorStore:
    def __init__(self, config: VectorStoreConfig, dimension: int): ...
    def add(self, chunks: list[Chunk], embeddings: np.ndarray) -> None: ...
    def load(self, name: str = "default") -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade embedding default and keep strict output-dimension validation</name>
  <files>embeddings.py, tests/test_embeddings.py</files>
  <behavior>
    - Test 1: Default model resolves to BAAI/bge-large-en-v1.5 (per D-05).
    - Test 2: Runtime fails fast when model output dimension differs from configured dimension (per D-06).
    - Test 3: Error message identifies expected vs actual dimensions clearly.
  </behavior>
  <action>Implement embedding default upgrade to BAAI/bge-large-en-v1.5 (per D-05) and preserve strict dimension verification at model load time (per D-06). Add focused tests that mock embedding outputs to validate fail-fast mismatch behavior with actionable errors.</action>
  <verify>
    <automated>pytest tests/test_embeddings.py -q</automated>
  </verify>
  <done>Embedding defaults are upgraded and dimension mismatches fail clearly before retrieval operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce vector index compatibility checks on add/load paths</name>
  <files>vector_store.py, tests/test_vector_store.py</files>
  <behavior>
    - Test 1: Adding embeddings with wrong width raises explicit dimension mismatch error (per D-06).
    - Test 2: Loading persisted index with incompatible configured dimension fails with clear remediation guidance (per D-06).
  </behavior>
  <action>Strengthen vector-store checks so embedding/index dimension mismatches cannot silently degrade retrieval quality (per D-06). Ensure add/load error messages include index dimension, embedding dimension, and recommended next action (rebuild index or correct config).</action>
  <verify>
    <automated>pytest tests/test_vector_store.py -q</automated>
  </verify>
  <done>Dimension incompatibilities are explicitly blocked and actionable across runtime and persisted index paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add migration-safe regression tests for index/data compatibility handling</name>
  <files>tests/test_vector_store.py, tests/test_embeddings.py</files>
  <behavior>
    - Test 1: Fresh index creation succeeds with upgraded embedding default.
    - Test 2: Incompatible persisted index fixtures fail predictably with expected message.
  </behavior>
  <action>Add regression tests covering fresh-build success and persisted-index mismatch failure patterns to protect migrations and future model upgrades.</action>
  <verify>
    <automated>pytest tests/test_embeddings.py tests/test_vector_store.py -q</automated>
  </verify>
  <done>Embedding upgrade is production-safe with explicit compatibility guardrails and regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model outputs->vector index | External model vectors cross into internal ANN index constraints |
| persisted FAISS index->runtime load | On-disk index state may not match configured dimensions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | vector_store add/load validation | mitigate | Explicit dimension checks before add/load to prevent index corruption or undefined behavior. |
| T-01-10 | D | startup path with mismatched index | mitigate | Fail fast with actionable error instead of partial startup with degraded retrieval. |
| T-01-11 | R | retrieval-quality regressions after model swap | mitigate | Add deterministic regression tests for fresh and mismatch paths with explicit expected outcomes. |
| T-01-12 | S | model identity/config drift | accept | Model provenance attestation deferred; v1 accepts configured model source with strict dimension checks. |
</threat_model>

<verification>
- Run: pytest tests/test_embeddings.py tests/test_vector_store.py -q
- Run: pytest tests -q
- Spot-check: pipeline initialization fails clearly when index/model dimensions diverge.
</verification>

<success_criteria>
- Default embedding model upgraded to BAAI/bge-large-en-v1.5.
- Dimension/index incompatibilities fail fast with clear remediation.
- Regression tests cover fresh and mismatch behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-03-SUMMARY.md`
</output>
