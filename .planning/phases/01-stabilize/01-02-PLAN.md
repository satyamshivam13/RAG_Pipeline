---
phase: 01-stabilize
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - document_loader.py
  - generator.py
  - tests/test_generator.py
  - tests/test_document_loader.py
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "Chunking respects semantic boundaries rather than fixed character windows."
    - "Generation enforces a hard context token budget and drops least-relevant chunks first when needed."
    - "Context truncation produces an explicit warning signal for debugging visibility."
  artifacts:
    - path: "document_loader.py"
      provides: "Semantic-aware chunk splitting preserving public API"
    - path: "generator.py"
      provides: "Token-budget enforcement and truncation telemetry"
    - path: "tests/test_document_loader.py"
      provides: "Semantic boundary regression checks"
  key_links:
    - from: "generator.py"
      to: "models.py"
      via: "GeneratorOutput metadata/warnings"
      pattern: "truncat|warning|context_used"
    - from: "document_loader.py"
      to: "main.py"
      via: "DocumentLoader.chunk_documents ingestion path"
      pattern: "chunk_documents"
---

<objective>
Introduce semantic chunking and enforce context token budgets safely.

Purpose: Prevent overflow failures and improve retrieval context quality while preserving ingestion/generation APIs.
Output: Updated loader/generator logic plus deterministic tests for chunking and truncation behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase-01-stabilize/01-CONTEXT.md
@document_loader.py
@generator.py
@tests/test_generator.py

<interfaces>
From document_loader.py:
```python
class DocumentLoader:
    def chunk_document(self, doc: Document) -> list[Chunk]: ...
```

From generator.py:
```python
class Generator:
    def generate(self, query: str, context_chunks: list[RetrievedChunk]) -> GeneratorOutput: ...
```

From models.py:
```python
class GeneratorOutput(BaseModel):
    answer: str
    context_used: list[RetrievedChunk]
    processing_time_ms: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement semantic-aware chunk splitting while preserving loader contracts</name>
  <files>document_loader.py, tests/test_document_loader.py</files>
  <behavior>
    - Test 1: Chunking prefers paragraph/sentence boundaries over fixed split points (per D-07).
    - Test 2: Existing loader APIs and overlap semantics remain backward-compatible.
    - Test 3: Extremely short trailing fragments below min size are safely dropped.
  </behavior>
  <action>Replace naive fixed-size chunking internals with semantic-aware breakpoint logic that prioritizes paragraph and sentence boundaries before hard fallback splitting (per D-07). Keep public methods and return types unchanged. Add tests that assert semantic boundaries and compatibility of overlap/min chunk behavior.</action>
  <verify>
    <automated>pytest tests/test_document_loader.py -q</automated>
  </verify>
  <done>Loader emits semantically cleaner chunks without API breakage for ingestion callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce hard generation token budget with relevance-ordered truncation</name>
  <files>generator.py, tests/test_generator.py</files>
  <behavior>
    - Test 1: When context exceeds budget, least-relevant chunks are truncated first (per D-08).
    - Test 2: Generation always runs within configured hard token budget (per D-08).
    - Test 3: Truncation emits explicit warning metadata/log signal (per D-09).
  </behavior>
  <action>Add token-budget management in generator context assembly using existing token counting utilities where available and deterministic fallback otherwise. Enforce a hard cap (per D-08), remove lowest-relevance chunks first, and emit explicit truncation warning metadata/logging for debugging visibility (per D-09).</action>
  <verify>
    <automated>pytest tests/test_generator.py -q</automated>
  </verify>
  <done>Generator cannot overflow configured context budget and surfaces truncation transparently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add integration-level regression for long-context query safety</name>
  <files>tests/test_generator.py, tests/test_document_loader.py</files>
  <behavior>
    - Test 1: Long synthetic context no longer causes context-window overflow errors.
    - Test 2: Truncation path is deterministic and testable under repeated runs.
  </behavior>
  <action>Extend tests with long-context fixtures that force truncation and validate deterministic context selection/truncation warning outputs. Keep test runtime fast by mocking LLM calls and limiting fixture size.</action>
  <verify>
    <automated>pytest tests/test_generator.py tests/test_document_loader.py -q</automated>
  </verify>
  <done>Long-context behavior is stable, predictable, and guarded by automated regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested documents->chunker | Untrusted raw text is transformed into chunk boundaries |
| retriever scores->generator truncation | Similarity scores influence what context reaches the LLM |
| generator->LLM API | Prompt payload size and content shape external API behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | document_loader.py splitting logic | mitigate | Deterministic split ordering and boundary tests to prevent adversarial malformed chunk boundaries. |
| T-01-06 | D | generator.py context assembly | mitigate | Hard token caps and bounded truncation loop to avoid oversized prompt failures. |
| T-01-07 | I | truncation warning/log metadata | mitigate | Emit concise warnings without logging full sensitive document payloads. |
| T-01-08 | E | relevance-ordered context selection | accept | Score poisoning risk accepted in v1; strict score validation and reranking are handled in later phases. |
</threat_model>

<verification>
- Run: pytest tests/test_document_loader.py tests/test_generator.py -q
- Run: pytest tests -q
- Spot-check: long-context query returns answer + visible truncation warning when budget is exceeded.
</verification>

<success_criteria>
- Semantic chunking replaces naive fixed splitting while preserving API compatibility.
- Hard context budget enforcement prevents overflow errors.
- Truncation warnings are explicit and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-02-SUMMARY.md`
</output>
