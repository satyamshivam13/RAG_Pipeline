---
phase: 01-stabilize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - models.py
  - main.py
  - evaluator_agent.py
  - tests/test_guardrail.py
  - tests/test_evaluator.py
  - tests/test_pipeline_phase1_runtime.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
must_haves:
  truths:
    - "User queries return without a blocking guardrail LLM stage."
    - "Evaluation runs in background by default and does not block answer return."
    - "A synchronous evaluator mode is available for deterministic tests/debugging."
  artifacts:
    - path: "main.py"
      provides: "Runtime path retrieval -> generation -> evaluation scheduling"
    - path: "config.py"
      provides: "Pipeline and evaluator toggles for async default + sync fallback"
    - path: "models.py"
      provides: "PipelineResult shape supporting deferred evaluation metadata"
  key_links:
    - from: "main.py"
      to: "retriever.py"
      via: "RetrieverConfig.similarity_threshold gate"
      pattern: "retrieve\("
    - from: "main.py"
      to: "evaluator_agent.py"
      via: "non-blocking dispatch by default"
      pattern: "Thread|executor|deferred|sync"
---

<objective>
Remove blocking runtime stages while preserving stable output contracts.

Purpose: Deliver immediate latency reduction and safer runtime behavior for Phase 1.
Output: Updated orchestration/config/models and tests for guardrail removal and deferred evaluator execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase-01-stabilize/01-CONTEXT.md
@main.py
@config.py
@models.py
@evaluator_agent.py
@tests/test_guardrail.py
@tests/test_evaluator.py

<interfaces>
From retriever.py:
```python
class Retriever:
    def retrieve(self, query: str) -> list[RetrievedChunk]: ...
```

From models.py:
```python
class PipelineResult(BaseModel):
    query: str
    answer: str
    retrieval: list[RetrievedChunk]
    generation: GeneratorOutput
    evaluation: EvaluatorOutput
```

From evaluator_agent.py:
```python
class EvaluatorAgent:
    def evaluate(self, answer: str, context_chunks: list[RetrievedChunk], query: str = "") -> EvaluatorOutput: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define runtime contracts for guardrail bypass and evaluator scheduling</name>
  <files>config.py, models.py</files>
  <behavior>
    - Test 1: Pipeline config exposes deferred evaluator by default and explicit sync fallback mode (per D-03, D-04).
    - Test 2: Pipeline result can represent deferred evaluator status without breaking existing fields.
  </behavior>
  <action>Introduce config flags that keep guardrail code available but disabled in default runtime path (per D-01). Add evaluator execution mode with default deferred behavior and explicit sync option for tests/debugging (per D-03, D-04). Update PipelineResult metadata to represent deferred evaluation completion/error state while preserving backward compatibility of existing fields.</action>
  <verify>
    <automated>pytest tests/test_evaluator.py -q</automated>
  </verify>
  <done>Config and model contracts clearly support default deferred evaluation and explicit sync fallback without deleting guardrail compatibility paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire query runtime path to retrieval-threshold gating + deferred evaluator</name>
  <files>main.py, evaluator_agent.py</files>
  <behavior>
    - Test 1: Query path executes retrieval to generation without invoking GuardrailAgent in default mode (per D-01, D-02).
    - Test 2: Default query returns answer even when evaluator work is still pending (per D-03).
    - Test 3: Sync evaluator mode returns deterministic evaluation output before query returns (per D-04).
  </behavior>
  <action>Update pipeline orchestration so retrieval score thresholding is the sole runtime gate (per D-02), using RetrieverConfig thresholds and preserving logging visibility. Keep guardrail module importable and callable behind compatibility/deprecation paths (per D-01). Add safe deferred evaluator execution with error isolation so evaluator failures never fail user answers, plus sync mode for deterministic runs (per D-03, D-04).</action>
  <verify>
    <automated>pytest tests/test_guardrail.py tests/test_evaluator.py -q</automated>
  </verify>
  <done>Runtime no longer blocks on guardrail/evaluator in default mode, evaluator errors are isolated, and sync fallback is functional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add runtime regression tests for blocking-stage removal and deferred behavior</name>
  <files>tests/test_pipeline_phase1_runtime.py, tests/test_guardrail.py, tests/test_evaluator.py</files>
  <behavior>
    - Test 1: Default query path does not execute guardrail stage and still returns grounded answer.
    - Test 2: Deferred evaluator mode returns answer before evaluation completion.
    - Test 3: Sync fallback mode blocks until evaluation completes and returns score fields.
  </behavior>
  <action>Create focused pipeline-level tests with mocked LLM calls to verify no guardrail-stage call in default runtime path and to prove deferred vs sync evaluator behavior. Keep tests deterministic and independent of network/API calls.</action>
  <verify>
    <automated>pytest tests/test_pipeline_phase1_runtime.py -q</automated>
  </verify>
  <done>Automated tests fail if blocking stages are reintroduced or evaluator mode semantics regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->pipeline query | Untrusted question text enters orchestrator and prompts |
| pipeline->LLM client | Untrusted composed context is sent to external model API |
| evaluator worker->shared state | Background worker writes evaluation metadata read by main response path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | main.py query orchestration | mitigate | Restrict stage control via typed config flags and explicit mode enum checks before dispatch. |
| T-01-02 | D | evaluator deferred worker | mitigate | Timeout and exception isolation in worker path; never block or crash main response path on evaluator failures. |
| T-01-03 | I | logs with user queries/context | mitigate | Keep existing redaction/minimal logging pattern; avoid dumping full context chunks in error logs. |
| T-01-04 | R | stage-mode observability | mitigate | Add mode/status fields in result metadata for auditable path reconstruction. |
</threat_model>

<verification>
- Run: pytest tests/test_pipeline_phase1_runtime.py tests/test_guardrail.py tests/test_evaluator.py -q
- Spot-check: a demo query returns answer when evaluator is delayed.
- Spot-check: sync mode returns deterministic evaluation fields before method return.
</verification>

<success_criteria>
- Default query path has no blocking guardrail LLM stage.
- Evaluator runs deferred by default with safe error isolation.
- Sync evaluator mode is available and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-01-SUMMARY.md`
</output>
